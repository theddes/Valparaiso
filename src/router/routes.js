import Index from '@/pages/Index'

const routes = [
    {
        path: '/',
        name: 'Index',
        component: Index,
        meta: {
            title: ''
        }
    }
]

export default routes
